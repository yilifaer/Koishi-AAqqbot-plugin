# 决策记录（DECISIONS）

记录 Koishi 端插件与项目所有者确认过的决定。新的决定追加在表格末尾；推翻旧决定时不要删除原行，新增一行并注明「取代第 N 条」。

AA 端（[aa-qqbot-plugin](https://github.com/yilifaer/aa-qqbot-plugin)）已经定好的决定照做，这里不重复：
踢人开关只在机器人这边（AA #11）、群名片第一期就做（AA #12）、新成员用验证码 / 老成员免验证（AA #13、#14）、
冲突永不处置（AA #14、#17）、不做私聊验证（AA #17）。接口以 AA 仓库的 `API.md` 为准。

本文件不包含任何群号、QQ 号、服务器地址或密钥。这些全部是插件的配置项，由使用者在 Koishi 控制台里填写。

| # | 日期 | 决定 | 理由 |
|---|---|---|---|
| 1 | 2026-09-25 | GitHub 仓库名 `Koishi-AAqqbot-plugin`；npm 包名 **`koishi-plugin-aaqqbot`**；许可证 **MIT**；**公开开源** | 所有者希望别的联盟也能用。npm 包名必须小写、以 `koishi-plugin-` 开头，Koishi 插件市场才能搜到 |
| 2 | 2026-09-25 | 因为开源：群号、机器人 QQ、网址、密钥、联盟简称**一律不写进代码和仓库**，全部做成配置项；`HANDOFF/` 交接文档**不提交**到仓库 | 仓库公开；交接文档含内部信息 |
| 3 | 2026-09-25 | 发布方式：**GitHub Actions 自动发布到 npm**。所有者在 GitHub 仓库设置里填 npm 发布令牌（一次），之后在 GitHub 上触发发布。令牌不经过 Claude | 所有者不用敲命令；凭据不进对话 |
| 4 | 2026-09-25 | 入群申请被 AA 明确判为 `deny`（取代开工说明 K2 的建议值）：**自动拒绝**，拒绝理由里附上 AA 绑定网址（网址和拒绝文字可在配置里设置）。以下情况**仍然不拒绝**，申请留给管理员并通知运维群：`review`、拿不到明确答案（网络错误、超时、非 200 等）、不认识的 `decision`、这个群处于熔断状态 | 所有者选择自动拒绝；「无法判断不处置」是红线 |
| 5 | 2026-09-25 | 每个受管群有 **4 档模式**：`off` 不管 / `report` 只在运维群报告，群成员感觉不到 / `remind` 在群里 @ 提醒 + 名片加标记，**不踢** / `enforce` 提醒 + 宽限期到了移出。**新群默认 `report`**。某群第一次改成 `enforce`（或 `remind`）前，至少要成功跑过一轮 `report`，并由管理员用命令确认 | 可以一步步升级，刚上线时不惊动成员；`remind` 是所有者提出「改名片 + 提醒」后由 Claude 建议、所有者同意增加的一档 |
| 6 | 2026-09-25 | 已在群里、被判 `deny` 的人：**宽限期 48 小时**（可配置），从第一次被发现开始算。宽限期内每天在群里 **@ 提醒一次**，时间默认 **19:30**（按机器人电脑的本地时间，可配置），提醒文字可配置。同一个群的多人合并成一条消息 | 所有者选择。群里 @ 比私聊可靠（非好友私聊可能被 QQ 风控拦截） |
| 7 | 2026-09-25 | 宽限期内还把这个人的群名片**前面加标记**，例如「【SPY】张三」。标记文字可配置；加上后超过长度上限就截掉后面的部分。他变成合格后，机器人自动改成 AA 给的正式名片 | 所有者提出 |
| 8 | 2026-09-25 | 成员自己改了群名片：**下次巡检时改回** AA 给的名片 | 以 AA 名片为准 |
| 9 | 2026-09-25 | 熔断：一轮里某个群要处置的人数**超过 5 人或超过群人数的 10%（取较小者）**时，这一轮这个群**一个都不处置**，只在运维群报警，等管理员用命令确认。「处置」包括**移出、拒绝申请、@ 提醒和改标记名片**。另外每群每小时最多移出 **10 人**，超过的留到下一轮。以上数字都可配置 | 防止 AA 误配置时把群踢光或把几十人名片改成【SPY】 |
| 10 | 2026-09-25 | 管理命令必须**同时满足三个条件**：Koishi 权限等级 ≥ 3；**并且** QQ 在配置的「运维名单」里；**并且**在运维群里或私聊机器人时使用。在受管群里发管理命令一律不理 | 交接文档 R13；「与」关系，不是「满足任一」 |
| 11 | 2026-09-25 | 运维通知发到所有者指定的**运维群**（群号在配置里填，不能是受管群）。每轮一条汇总，不逐人刷屏 | 所有者已有运维群 |
| 12 | 2026-09-25 | `enforce` 群移出成员后，**在该群里也发一条公告**（文字可配置，可关闭），同时在运维群汇报 | 所有者选择 |
| 13 | 2026-09-25 | 机器人重新上线时，**补处理**掉线期间还挂着的入群申请，第一期就做。依赖 LLBot 的接口，实测不可用时自动跳过并在运维群说明 | 所有者选择 |
| 14 | 2026-09-25 | 群成员邀请别人入群、且群设置了「邀请也要审核」时（申请里没有验证信息）：**做成配置项**，可选「和普通申请一样问 AA」或「一律留给管理员」。默认「和普通申请一样」 | 所有者要求可以在设置里选 |
| 15 | 2026-09-25 | 移出成员时**不拉黑**（`reject_add_request = false`），他补好绑定后还能重新申请 | 交接文档 R8：拉黑会把误判变成永久的 |
| 16 | 2026-09-25 | 测试环境：测试机器人 QQ、测试群（测试机器人是管理员）、可被踢的小号都已具备。测试用的 Koishi 装在 **Mac mini 上另开的第二个实例**里，用测试机器人 QQ 登录，**不碰正式实例** | 所有者提供。Mac mini 是正式机器人所在的电脑 |
| 17 | 2026-09-25 | 分工：Claude 写代码和自动化测试（用模拟的 AA 服务器），发布到 npm；所有者在家里的测试环境安装、按步骤实测，并反馈结果 | Claude 所在的云端连不到所有者家里的网络 |
| 18 | 2026-09-25 | 所有者要做的事（上线前）：每个受管群的加群方式设为「**需要发送验证信息**」；正式机器人上的旧 QQMonitor 插件保持禁用，直到新插件上线 | 开工说明 K9；交接文档 §11 |
| 19 | 2026-09-25 | **以下第 19–26 条是写代码时 Claude 按安全原则定的细节，所有者可以推翻。** report 模式下，AA 判定 `allow` 的入群申请**照样自动同意**；除此之外 report 模式不改动群里任何东西（不拒绝、不 @、不改名片、不移出） | 同意合格的人不会伤害任何人，和人工管理员的做法一样；report 的目的是「先观察」 |
| 20 | 2026-09-25 | 群名片同步（包括改回成员自己改掉的名片）只在 remind / enforce 模式进行；report 模式只在报告里写「有 N 人的名片与 AA 不一致」 | 与第 19 条一致：report 模式群成员感觉不到任何变化 |
| 21 | 2026-09-25 | 宽限期（第 6 条的 48 小时）从这个群**进入 enforce 之后**第一次发现这个人不合格时开始算；remind 模式只提醒、没有截止时间。从 enforce 降回 remind 时截止时间清空，再升回 enforce 时重新给完整的 48 小时 | 保证每个人在被移出前都收到过带截止时间的提醒 |
| 22 | 2026-09-25 | 巡检发现的不合格成员在每天的提醒时间统一 @；**直接进群**（没经过申请）且不合格的新人，进群时立即 @ 一次 | 避免凌晨巡检时 @ 人；新人刚进群时提醒最有效 |
| 23 | 2026-09-25 | 熔断只计算「这一轮**新**发现的不合格」，已经在宽限期里的人不算；阈值最小为 1（小群里移出 1 个人不需要每次都确认，2 个及以上才熔断）。熔断后一直保持，直到管理员 `aaqq.confirm`；确认后的下一轮不再触发熔断 | 否则刚上线时每一轮都会因为同一批没绑定的老成员熔断 |
| 24 | 2026-09-25 | 升级到 remind / enforce 还没确认时，继续按**已经确认过的模式**执行（第一次就是 report）；降级立即生效，降级后再升级需要重新确认。降到 report 时撤掉以前加的名片标记、清空宽限记录 | 未确认的升级不生效，但也不打乱已经在进行的提醒 |
| 25 | 2026-09-25 | 暂停（`aaqq.pause`）= 一切改动都停：不审批、不拒绝、不提醒、不改名片、不移出，也不拉取 AA 的变化；恢复后立即巡检一轮补上。暂停状态存在数据库里，重启后仍然有效 | 紧急刹车必须彻底 |
| 26 | 2026-09-25 | 群主、管理员、QQ 官方机器人号段（`is_robot`）、这个 Koishi 里的所有机器人账号、白名单：永不提醒、永不改名片、永不移出。机器人只是管理员（不是群主）时，也不改其他管理员的名片 | 交接文档 R7；QQ 里管理员改不了其他管理员的名片 |
| 27 | 2026-09-25 | **取代第 21 条**（代码审查后修改）：enforce 模式下，截止时间在**第一次成功发出带截止时间的 @ 提醒**时才定下（那一刻 + 宽限期）；移出前 **36 小时内必须成功提醒过**这个人。提醒没发出去、熔断或暂停期间没有提醒，都不会被移出 | 审查发现：熔断几天后一确认，没收到过提醒的人会被立刻移出 |
| 28 | 2026-09-25 | 补充第 23 条（代码审查后修改）：①熔断也看「这一轮到期要移出」的人数（没被确认过的），超过阈值同样熔断；②`aaqq.confirm` 的豁免 **1 小时内有效**，而且只覆盖确认时报告里的人数，更多就照样熔断；③确认要求 12 小时内的巡检报告 | 审查发现：确认后那一轮巡检失败时，豁免会一直留着，之后 AA 出错也不熔断 |
| 29 | 2026-09-25 | 白名单里的 QQ 申请入群、AA 判不合格时，不自动拒绝，留给管理员。机器人重新上线后**补处理**的申请，合格的照样同意，不合格的**不自动拒绝**（LLBot 的积压列表分不出是不是邀请入群） | 白名单的人不能被处置；补处理时信息不全，保守处理 |
| 30 | 2026-09-25 | 其他安全细节：先移出、再改名片，移出前再问一次 AA；每个群每轮最多改 100 张名片（其余下一轮）；身份认不出来的成员按受保护处理；无法判断的人数超过熔断阈值时这一轮不改动；群改成 off 时撤掉标记、清空宽限记录；群从 AA 上移除时清空确认状态，加回来要重新确认 | 代码审查结果 |
| 31 | 2026-09-25 | **取代第 16 条的测试环境**：所有者明确同意，测试在 **Mac mini 上正式的 Koishi 和正式机器人**上进行，AA 网址指向树莓派测试 AA。条件：测试 AA 上只添加测试群；运维群号填测试运维群；旧 QQMonitor 插件保持禁用 | 所有者选择；插件只管 AA 列出来的群，其他群一概不碰（有自动测试） |
| 32 | 2026-09-25 | 事件游标和受管群列表按「AA 网址」分开保存：从测试 AA 切换到正式 AA 后自动从头开始，不沿用测试 AA 的进度 | 否则会拿测试 AA 的游标去读正式 AA，漏掉一批变化 |
| 33 | 2026-09-25 | 取群成员名单时强制 LLBot 刷新（`no_cache`）；名单比上一轮少了超过 5 人且超过 10% 时，这一轮不作为完整名单交给 AA，也不取消任何人的跟踪，下一轮名单稳定后恢复 | 第二轮问题清单第 1 条：不完整的名单会让 AA 删掉老成员的「免验证」资格 |
| 34 | 2026-09-25 | 启动时先安排好定时任务，每个启动步骤单独出错不影响其他；读不到暂停状态时按「暂停」处理。一轮巡检跑满 20 分钟就在两个群之间停下，剩下的群马上接着巡检（硬上限 40 分钟） | 第二轮问题清单第 2、3 条 |
| 35 | 2026-09-25 | 第二轮问题清单第 4–7 条暂不处理：④AA 数据库还原后事件编号重来——插件这边判断不出来，需要改 AA 接口，每 6 小时的巡检会兜底；⑤插件重启本来就会马上完整巡检；⑥最多让移出晚一点，不会误踢；⑦网址含中文或空格时只会连不上，不会误踢。第 8 条（补处理申请）加进测试步骤 7.1 真机验证 | 所有者要求不重要的问题可以忽略 |
| 36 | 2026-09-25 | 以后的改动都通过 Pull Request 提交，所有者在 GitHub 上点「Squash and merge」合并（提交显示 Verified，作者是所有者）。发布改用 npm 的「受信任发布」，不再需要令牌 | 所有者同意 |
